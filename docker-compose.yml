version: '3.3'


services:
  backend:
    build:
      context: .
      dockerfile: app/Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"

#    devices:
#      - /dev/video0:/dev/video0
